# IronLog_web

A premium, serverless single-page web application converted from the IronLog iOS App. 

## Features
- Fully client-side storage utilizing browser `localStorage`.
- Scientific weight training periodization target formulas (Light, Medium, Heavy, Deload).
- Full interoperability with iOS backup JSON formats (alternating array dictionary key schemas and Cocoa date parsing).
